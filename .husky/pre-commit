#!/usr/bin/env sh
set -eu

# Pre-commit quality gates (fast and reliable)
# Keep commit-time checks focused on staged code and static analysis.

printf '%s\n' "🔍 Running pre-commit quality gates..."

# Phase 1: Staged file formatting/linting
printf '\n📋 Phase 1: Staged Formatting & Linting\n'
npx lint-staged

# Phase 2: Fast static analysis
printf '\n📋 Phase 2: Fast Static Analysis\n'
npm run type-check
npm run deadcode:check
npm run dup:all

printf '\n✅ Pre-commit quality gates passed!\n'
