/**
 * Type Definitions Index
 *
 * Centralized re-exports for all application types.
 * Import from specific subdirectories for better tree-shaking.
 *
 * @module types
 */

// Domain types (business logic)
export * from "./domain";

// UI types (component props, state)
export * from "./ui";

// API types (request/response)
export * from "./api";
