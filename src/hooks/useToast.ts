/**
 * @deprecated Import from @/providers/ToastProvider instead
 *
 * useToast is actually a provider with context, not just a hook.
 * It has been moved to the providers directory for better organization.
 */
export { ToastProvider, useToast } from "@/providers/ToastProvider";
